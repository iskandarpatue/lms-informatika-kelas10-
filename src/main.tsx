import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // Pastikan file program LMS utama Anda bernama App.tsx
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  
    
  ,
)
